
Django==4.2
Pillow==9.2.0  # For handling image uploads, if applicable
qrcode==7.3.1  # For generating QR codes
djangorestframework==3.14.0  # For creating APIs (e.g., JSON responses)
django-widget-tweaks==1.4.12  # Optional, for form rendering customization

# AR-related libraries (depends on the use case, you might need to install a JS framework separately)
pythreejs==2.3.0  # Optional: For rendering 3D models in Django

gunicorn==20.1.0  # Optional, for production deployment
psycopg2-binary==2.9.6

